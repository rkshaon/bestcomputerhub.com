# Project Settings
PROJECT_NAME_SETTINGS = 'E-commerce'

# Database Settings
DATABASES_SETTINGS = {
    'default': {
        'ENGINE': 'django.db.backends.postgresql_psycopg2',
        'NAME': 'db_name',
        'USER': 'db_username',
        'PASSWORD': 'db_password',
        'HOST': 'localhost',
        'PORT': '5432',
    }
}

# Host
ALLOWED_HOSTS_SETTINGS = []

# Security
SECRET_KEY_SETTINGS = 'projectssecretekey'

# Cors
CORS_ALLOWED_ORIGINS_SETTINGS = [
    "http://localhost:8080",
    "http://127.0.0.1:8080",
]
CSRF_TRUSTED_ORIGINS_SETTINGS = [
    "http://localhost:8080",
    "http://127.0.0.1:8080",
    "http://localhost:5000",
    "http://localhost:5173",
]

X_FRAME_OPTIONS_SETTINGS = 'ALLOW-FROM http://localhost:8080'

# Frontend
FRONTEND_BASE_URL_SETTINGS = ''
